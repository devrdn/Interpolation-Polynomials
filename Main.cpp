#include "include/header.h"

double f(double x) {
    return sqrt(2 - 3 * x);
}


int main()
{
    vector<double> xi{ -7. / 3, -2. / 3, 1. / 3 };
    vector<double> zi{ 0.0, -3.0 };
    cout << "Hello World!\n";
}
